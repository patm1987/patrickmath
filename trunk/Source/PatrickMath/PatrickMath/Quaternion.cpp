#include "stdafx.h"
#include "Quaternion.h"

const Quaternion::Container identity = {0.f, 0.f, 0.f, 1.f};
const Quaternion::Container zero = {0.f, 0.f, 0.f, 0.f};

const Quaternion IDENTITY (identity);
const Quaternion ZERO (zero);
